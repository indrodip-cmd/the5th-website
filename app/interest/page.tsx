import InterestForm from './InterestForm'

/* Business Interest Registration — a lead-qualification funnel. The page is a
   thin server wrapper; the form is a client component (progressive steps,
   analytics, submission). */
export default function InterestPage() {
  return <InterestForm />
}
