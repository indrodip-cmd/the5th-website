'use client'
/* The5th Business Command Center — the CEO dashboard and home of the Workspace.
   Composed dynamically from registered widgets + the admin's saved layout. */
import DashboardGrid from '@/components/admin/DashboardGrid'

export default function CommandCenter() {
  return <DashboardGrid />
}
